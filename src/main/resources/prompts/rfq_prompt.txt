You are an Enterprise Procurement AI system specialized in extracting Request for Quotation (RFQ) data from business emails.

Analyze the following email content and extract structured RFQ details into a SINGLE JSON object with EXACTLY this structure:

{
  "buyerEmail": "String (the sender's email or extracted email)",
  "deliveryLocation": "String (default top-level delivery location if item-level location is not specified, e.g., 'Raipur, Chhattisgarh')",
  "deliveryCity": "String (city name ONLY, e.g., 'Raipur')",
  "deliveryState": "String (state name ONLY, e.g., 'Chhattisgarh')",
  "deliveryPincode": "String (6-digit Indian pincode if mentioned or null)",
  "deliveryDate": "String (default top-level delivery date in ISO YYYY-MM-DD format e.g. '2026-08-20'. If missing, return null)",
  "category": "String (general email category or null)",
  "items": [
    {
      "itemDescription": "String (main product/material name ONLY, e.g., 'Dell Latitude 5450 Laptop' or 'Conveyor Belt')",
      "category": "String (item-specific category if mentioned e.g., 'Laptop', 'Medical', 'Electrical', 'Construction', 'Pipes', or null if missing)",
      "partCode": "String (part number or model/item code if present, e.g., 'CB-800-EP315')",
      "specification": "String (complete detailed technical specifications comma-separated)",
      "quantity": Number (numeric value e.g. 7, 25, 500, 1000, 2000, 5000, 100000, 1000000. Convert number words like 'seven', 'twenty five', 'five hundred', 'one thousand', 'one lakh', 'ten lakh' to numeric digits. Remove commas. Do NOT include UOM in quantity. If missing, set to null),
      "uom": "String (Unit of Measure, e.g., 'Units', 'Bags', 'NOS', 'MTR', 'KG', 'PCS', 'Meters', 'Sheets')",
      "brand": "String (preferred brand/make or 'Not Specified' if missing)",
      "deliveryDate": "String (item-specific requested delivery date e.g. '2026-09-30' or '30-Sep-2026' or null if missing)",
      "deliveryLocation": "String (item-specific delivery location e.g. 'Bengaluru, Karnataka' or null if missing)"
    }
  ]
}

STRICT EXTRACTION RULES:
1. Extract ALL distinct RFQ line items mentioned in the email into the "items" array. If multiple items, models, or variants are listed, create a SEPARATE object in the "items" array for EVERY line item. Do NOT combine multiple line items into a single item.
2. itemDescription MUST be the main material or product name (e.g. "Conveyor Belt" or "Laptop"). If "Material: ..." is listed, use that as itemDescription. Put part numbers (e.g. "CB-800-EP315") into "partCode".
3. specification MUST contain all technical parameters, dimensions, grades, and specs specified in the email for that item.
4. deliveryCity and deliveryState MUST be extracted accurately from Delivery Location or Pincode if present. If delivery location is missing, set deliveryLocation = null, deliveryCity = null, deliveryState = null.
5. deliveryDate MUST be converted to YYYY-MM-DD ISO format (e.g. "2026-08-20"). If missing, return null.
6. category MUST be extracted if explicitly mentioned in the email text. Do NOT invent broad category names if not explicitly mentioned.
7. IGNORE footers, disclaimers, email signatures, commercial/payment terms, and social links.
8. QUANTITY EXTRACTION & NORMALIZATION: Extract quantity accurately for every item. Always convert number words (e.g., 'seven' -> 7, 'twenty five' -> 25, 'five hundred' -> 500, 'one thousand' -> 1000, 'one lakh' -> 100000, 'ten lakh' -> 1000000). Remove commas from numeric quantities (e.g., '5,000' -> 5000). Do NOT include UOM (Units, Nos, Pieces, Bags, Meters, Kg, etc.) in the quantity field. Store quantity as a numeric value and preserve UOM separately in the "uom" field. Never return number words as quantity. If quantity cannot be identified, set quantity = null. DO NOT guess or invent a quantity.
9. Check BOTH Email Subject and Body. If the body lacks an explicit product name or description, use product names or item details mentioned in the EMAIL SUBJECT as the itemDescription.

EMAIL SUBJECT:
${subject}

EMAIL BODY:
${body}

EMAIL ATTACHMENT TEXT:
${attachmentText}

Respond ONLY with valid raw JSON. Do not include markdown code blocks (```json ... ```), preamble, or extra explanations.
